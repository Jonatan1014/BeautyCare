<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BeautyCare" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/mainlight</item>
    </style>

    <style name="Theme.BeautyCare" parent="Base.Theme.BeautyCare" />

    <style name="BottomNavigation">
        <item name="android:background">@color/white</item>
        <item name="itemIconTint">@color/dark_dark</item>
        <item name="itemTextColor">@color/dark_dark</item>
        <item name="fontFamily">@font/montserrat</item>
    </style>

    <style name="mainstyle">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="edittext_bkgtheme">
        <item name="android:backgroundTint">@color/entra_sesion_</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="layaout_theme">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="bienvenido_theme">
        <item name="android:textColor">@color/black</item>

        <item name="android:gravity">center</item>
        <item name="fontFamily">@font/montserrat</item>
    </style>



</resources>